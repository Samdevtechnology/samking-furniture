import ImageTitle from "@/components/ImageTitle";
import CategoryHeader from "./components/CategoryHeader";

const page = () => {
  return (
    <div>
      <ImageTitle />
      <section>
        <CategoryHeader />
      </section>
    </div>
  );
};

export default page;
