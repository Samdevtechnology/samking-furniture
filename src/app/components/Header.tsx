import TopNav from "../../components/TopNav";
import NavBar from "./NavBar";

const Header = () => {
  return (
    <header>
      <TopNav />
      <NavBar />
    </header>
  );
};

export default Header;
